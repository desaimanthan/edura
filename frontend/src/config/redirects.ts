// Configuration for external redirect links
// Update these URLs to change where the redirects point to

export const redirectLinks = {
  // Video demo redirect
  'video-demo': 'https://www.youtube.com/watch?v=ISOqDeEmXOw&ab_channel=ManthanDesai',
  
  // Add more redirects here as needed
  // 'another-path': 'https://example.com',
};

export type RedirectKey = keyof typeof redirectLinks;

